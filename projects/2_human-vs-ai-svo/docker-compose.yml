services:
  pipeline:
    build:
      context: .
    volumes:
      # This project folder → /project inside the container
      - .:/project
      # STARK tool → /stark inside the container (read-only)
      - /mnt/local-hddpool/niveshull/STARK:/stark:ro
      # Raw .conllu corpus files → where the script expects them
      - /mnt/local-hddpool/niveshull/nh_svo_jun_2026:/project/data/raw/nh_svo_jun_2026
    environment:
      - STARK_PY=/stark/stark.py
      - MPLCONFIGDIR=/tmp/mplconfig
